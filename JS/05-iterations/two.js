// While loop
let index = 0
while (index < 3) {
    // console.log('inside loop');
    index++
}

const myArr = ['batman', 'superman', 'flash']
let arr = 0
while (arr < myArr.length) {
    // console.log('vallue is ' + myArr[arr]);
    arr++
}